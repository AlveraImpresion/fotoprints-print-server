{
  "name": "fotoprints-public-server",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor publico para pedidos, emails y administracion de FotoPrints.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
